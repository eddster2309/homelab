apiVersion: v1
kind: Service
metadata:
  name: speedtest-exporter
  labels:
    app: speedtest-exporter
spec:
  selector:
    app: speedtest-exporter
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
