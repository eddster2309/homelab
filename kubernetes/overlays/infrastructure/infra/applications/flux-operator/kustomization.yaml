# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - ../../../../../bases/flux-operator
  - oidc-secret.yaml
patches:
  - target:
      kind: HelmRelease
      name: flux-operator
    path: flux-operator-patch.yaml
