apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-nfs
spec:
  chart:
    spec:
      chart: csi-driver-nfs
      version: "4.13.4"
      # Lets Renovate resolve the pinned version; must match the base.
      sourceRef:
        kind: HelmRepository
        name: csi-driver-nfs
        namespace: flux-system
