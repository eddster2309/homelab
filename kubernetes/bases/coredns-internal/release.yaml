apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: coredns
spec:
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  interval: 30m
  chart:
    spec:
      chart: coredns
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: coredns-internal
        namespace: flux-system
      interval: 12h
