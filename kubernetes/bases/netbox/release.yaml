apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: netbox
spec:
  interval: 30m
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  chart:
    spec:
      chart: netbox
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: netbox
        namespace: flux-system
      interval: 12h
