apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: versitygw
spec:
  interval: 30m
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  releaseName: versitygw
  chartRef:
    kind: OCIRepository
    name: versitygw
    namespace: flux-system
