apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: karakeep
spec:
  install:
    remediation:
      retries: -1
  upgrade: 
    remediation:
      retries: -1
  interval: 30m
  chart:
    spec:
      chart: karakeep
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: karakeep
        namespace: flux-system
      interval: 12h