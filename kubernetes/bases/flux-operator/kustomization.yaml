apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- release.yaml
# No namespace.yaml: the operator runs in flux-system, which is already owned by
# the bootstrapped flux-system Kustomization (clusters/infra/flux-system).
