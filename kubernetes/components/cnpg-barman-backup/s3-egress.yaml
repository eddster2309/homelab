apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-cnpg-s3-egress
spec:
  podSelector:
    matchLabels:
      cnpg.io/podRole: instance
  policyTypes:
  - Egress
  egress:
  - ports:
    - port: 443
      protocol: TCP
